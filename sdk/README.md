# @cloudops-tools/sdk

`@cloudops-tools/sdk` is the backend/core library for CloudOps Tools.

It contains the AWS interaction layer (clients, services, operations, credential handling, and
core business logic). The CLI consumes this SDK to provide the end-user interface.

Use the SDK directly when you need programmable access to CloudOps workflows without the CLI.
